<div id="{{ html_id }}" class="inv-shortcode-video {{ class }}">
    <iframe src="{{ url }}" frameborder="0" width="{{ width }}" height="{{ height }}" frameborder="0"
            webkitAllowFullScreen mozallowfullscreen allowFullScreen></iframe>
</div>
